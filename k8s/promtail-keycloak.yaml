# Promtail for Keycloak logs
# In Kubernetes, Keycloak logs are captured via the pod's stdout/stderr
# This ConfigMap + DaemonSet scrape config specifically targets Keycloak pods
apiVersion: v1
kind: ConfigMap
metadata:
  name: promtail-keycloak-k8s-config
  namespace: nitte
data:
  config.yml: |
    server:
      http_listen_port: 9081
      grpc_listen_port: 0
      log_level: warn

    positions:
      filename: /tmp/positions-keycloak.yaml

    clients:
      - url: http://loki:3100/loki/api/v1/push
        tenant_id: keycloak-admin
        headers:
          X-Promtail-Api-Key: promtail-loki-secret

    scrape_configs:
      - job_name: keycloak-logs
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names:
                - nitte
        relabel_configs:
          - source_labels: ['__meta_kubernetes_pod_label_app']
            regex: keycloak
            action: keep
          - source_labels: ['__meta_kubernetes_pod_node_name']
            target_label: '__host__'
          - source_labels: ['__meta_kubernetes_namespace']
            target_label: namespace
          - source_labels: ['__meta_kubernetes_pod_name']
            target_label: pod
          - source_labels: ['__meta_kubernetes_pod_container_name']
            target_label: container
          - replacement: keycloak
            target_label: service
          - replacement: keycloak
            target_label: job
        pipeline_stages:
          - cri: {}
          - labeldrop:
              - filename
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: promtail-keycloak
  namespace: nitte
  labels:
    app: promtail-keycloak
spec:
  selector:
    matchLabels:
      app: promtail-keycloak
  template:
    metadata:
      labels:
        app: promtail-keycloak
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      tolerations:
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      containers:
      - name: promtail-keycloak
        image: grafana/promtail:3.1.0
        imagePullPolicy: IfNotPresent
        args: ["-config.file=/etc/promtail/config.yml"]
        ports:
        - containerPort: 9081
        volumeMounts:
        - name: promtail-config
          mountPath: /etc/promtail/config.yml
          subPath: config.yml
          readOnly: true
        - name: pods-logs
          mountPath: /var/log/pods
          readOnly: true
        - name: containers-logs
          mountPath: /var/log/containers
          readOnly: true
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
      volumes:
      - name: promtail-config
        configMap:
          name: promtail-keycloak-k8s-config
          items:
          - key: config.yml
            path: config.yml
      - name: pods-logs
        hostPath:
          path: /var/log/pods
      - name: containers-logs
        hostPath:
          path: /var/log/containers